# Python 콘솔 퀴즈 게임 과제 요구사항

## 1. 미션 개요

Python 기본 문법과 객체 지향 구조를 활용해 터미널에서 실행되는 퀴즈 게임을 처음부터 끝까지 구현한다. 입력과 출력의 흐름을 만들고, 역할에 따라 클래스를 나누며, JSON 파일에 퀴즈와 점수를 저장해 프로그램을 다시 실행해도 데이터가 유지되도록 한다.

Git으로 기능별 변경 이력을 관리하고, 별도 브랜치에서 작업한 기능을 병합하며, 완성한 프로젝트를 GitHub 공개 저장소에 업로드한다. 이를 통해 이후 브랜치 전략, Pull Request, 코드 리뷰와 같은 팀 협업의 기초를 익힌다.

## 2. 최종 결과물

다음 결과물을 완성하고 제출한다.

1. 아래 필수 기능과 구조를 갖춘 Python 콘솔 퀴즈 게임 1개
2. 프로젝트 코드와 작업 이력이 올라간 GitHub 저장소 1개
3. 다음 제출 자료
   - GitHub 저장소 URL
   - 개발 환경 설정 화면 캡처(예: VSCode, Python 버전, Git 설정)
   - 프로그램 실행 결과 캡처(퀴즈 추가, 목록, 플레이, 점수)
   - `git log --oneline --graph` 실행 결과 캡처

## 3. 학습 목표

과제를 마친 학습자는 다음 내용을 스스로 설명하고 활용할 수 있어야 한다.

### 3.1 Python 기초

- 변수의 의미와 사용 이유
- `int`, `str`, `bool`, `list`, `dict`의 차이
- `if/elif/else`를 이용한 조건별 처리
- `for`와 `while`의 차이와 상황에 맞는 선택
- 함수 정의, 매개변수, 반환값 활용

### 3.2 클래스와 객체

- 클래스의 의미와 사용 이유
- `__init__` 메서드와 `self`의 역할
- 속성(attribute)과 메서드(method)의 정의 및 활용

### 3.3 파일 입출력

- 파일을 열고 읽고 쓰는 기본 과정
- JSON 형식의 의미와 데이터 저장에 사용하는 이유
- `try/except`를 이용한 오류 처리

### 3.4 Git 기초

- Git의 의미와 필요성
- `init`, `add`, `commit`, `push`, `pull`, `checkout`, `clone`의 역할
- 브랜치 생성과 병합
- 원격 저장소 복제와 변경 사항 가져오기

## 4. 개발 환경과 제약 사항

### 4.1 개발 환경

- Python 3.10 이상을 사용한다.
- 외부 라이브러리는 사용하지 않는다.
- Python 표준 라이브러리는 사용할 수 있다.

### 4.2 코드 구조

- 모든 코드를 하나의 함수에 작성하지 않고 기능별 함수 또는 메서드로 분리한다.
- 역할이 구분된 클래스를 최소 2개 정의한다. 필수 클래스는 `Quiz`와 `QuizGame`이다.
- 입력 처리, 게임 진행, 파일 저장·불러오기 등 기능별 로직을 분리한다.

### 4.3 데이터 저장

- 데이터 파일은 프로젝트 루트의 `state.json`을 사용한다.
- 파일은 UTF-8 인코딩으로 저장하고 불러온다.
- 퀴즈 데이터와 최고 점수가 프로그램 종료 후에도 유지되어야 한다.
- JSON의 키 이름과 구조는 구현 전반에서 일관되게 유지한다.

최소 데이터 구조는 다음 형태를 참고할 수 있다. 키 이름이나 세부 구조는 변경할 수 있지만, 퀴즈 목록과 최고 점수를 표현해야 한다.

```json
{
  "quizzes": [
    {
      "question": "Python의 창시자는?",
      "choices": ["Guido", "Linus", "Bjarne", "James"],
      "answer": 1
    }
  ],
  "best_score": 3
}
```

## 5. 필수 기능 요구사항

### 5.1 메뉴와 프로그램 흐름

- 프로그램 실행 시 메뉴를 출력한다.
- 사용자는 번호를 입력해 다음 기능을 선택할 수 있어야 한다.
  1. 퀴즈 풀기
  2. 퀴즈 추가
  3. 퀴즈 목록
  4. 점수 확인
  5. 종료
- 프로그램 재시작시 추가한 퀴즈와 최고점수가 유지된다.
- 잘못된 입력을 처리하고 사용자가 다시 입력할 수 있게 한다.

### 5.2 공통 입력 및 예외 처리

숫자 입력이 필요한 모든 곳에서 최소한 다음 상황을 처리한다.

- 입력값의 앞뒤 공백을 제거한 뒤 처리한다.
- 빈 입력에는 안내 메시지를 출력하고 재입력 흐름으로 돌아간다.
- `abc`와 같이 숫자로 변환할 수 없는 입력에는 안내 메시지를 출력하고 재입력 흐름으로 돌아간다.
- 메뉴 `9`, 정답 `0`과 같이 허용 범위를 벗어난 숫자에는 안내 메시지를 출력하고 재입력 흐름으로 돌아간다.
- 실행 중 `KeyboardInterrupt`(`Ctrl+C`) 또는 `EOFError`가 발생해도 비정상 종료되지 않게 한다. 안내 메시지를 출력하고, 가능한 범위에서 데이터를 저장한 뒤 안전하게 종료한다.

데이터 파일과 관련된 예외도 처리한다.

- `state.json`이 없으면 기본 퀴즈 데이터로 실행한다.
- 파일이 손상되었으면 안내 메시지를 출력하고 기본 퀴즈 데이터로 복구하거나 초기화한다.
- 파일 읽기 또는 쓰기 오류를 `try/except`로 처리한다.

### 5.3 `Quiz` 클래스

개별 퀴즈를 나타내는 `Quiz` 클래스를 정의한다.

- 필수 속성
  - 문제: `question`
  - 선택지: `choices`
  - 정답: `answer`
- 선택지는 기본 4개로 구성한다.
- 정답은 1~4 중 하나의 번호로 관리하는 방식을 권장하며, 다른 방식을 택할 경우에도 전체 구현에서 일관성을 유지한다.
- 퀴즈 출력, 정답 확인 등 개별 퀴즈에 필요한 메서드를 구현한다.

### 5.4 기본 퀴즈 데이터

- 본인이 선택한 하나의 주제로 퀴즈를 5개 이상 직접 작성한다.
- 각 퀴즈는 문제, 선택지 4개, 정답을 포함한다.
- 기본 퀴즈는 `Quiz` 클래스의 인스턴스로 생성한다.

### 5.5 퀴즈 풀기

- 저장된 퀴즈를 출제한다.
- 사용자가 각 문제의 정답을 입력할 수 있어야 한다.
- 입력한 답의 정답 또는 오답 여부를 알려준다.
- 모든 문제를 풀면 정답 수와 결과를 표시한다.
- 퀴즈가 하나도 없는 경우를 처리한다.
- 이 기능은 `main` 이외의 별도 브랜치에서 작업한 뒤 `main`에 병합한다.

### 5.6 퀴즈 추가

- 사용자가 새로운 퀴즈를 등록할 수 있어야 한다.
- 문제, 선택지 4개, 정답 번호를 입력받는다.
- 공통 입력 및 예외 처리 기준을 적용한다.
- 추가한 퀴즈를 `state.json`에 저장한다.

### 5.7 퀴즈 목록

- 저장된 퀴즈 목록을 확인할 수 있어야 한다.
- 퀴즈가 하나도 없는 경우를 처리한다.

### 5.8 점수 확인

- 최고 점수를 확인할 수 있어야 한다.
- 퀴즈를 풀 때마다 기존 최고 점수와 비교하고 필요한 경우 갱신한다.
- 최고 점수를 `state.json`에 저장한다.
- 아직 퀴즈를 풀지 않은 경우를 처리한다.

최고 점수는 점수 또는 최고 정답 수 등 일관된 방식으로 관리할 수 있다.

### 5.9 `QuizGame` 클래스

게임 전체를 관리하는 `QuizGame` 클래스를 정의한다.

- 필수 속성
  - 퀴즈 목록
  - 최고 점수
- 다음 역할을 메서드로 분리한다.
  - 메뉴 표시 및 선택 처리
  - 퀴즈 풀기
  - 퀴즈 추가
  - 퀴즈 목록 보기
  - 점수 확인
  - 파일 저장 및 불러오기

## 6. Git 및 GitHub 요구사항

### 6.1 저장소 설정

- GitHub에 새 저장소를 만들고 프로젝트 코드를 업로드한다.
- 로컬 Git 저장소를 설정한다.
- `.gitignore`와 `README.md`를 생성한다.
- 초기 설정을 마친 뒤 첫 번째 `commit`과 `push`를 수행한다.
- 최종 GitHub 저장소는 공개 상태로 둔다.

### 6.2 작업 이력

- 의미 있는 커밋을 최소 10개 만든다.
- 메뉴, `Quiz`, 기본 데이터, 플레이, 추가, 목록, 점수, 파일 입출력, 클래스 구조 정리, README 등 기능 단위로 커밋한다.
- 각 커밋 메시지에는 변경 내용을 알 수 있는 요약을 포함한다.
- 형식적인 메시지를 피하고 다음과 같이 작업이 드러나는 형식을 권장한다.
  - `Feat: 퀴즈 출제 기능 구현`
  - `Fix: 점수 계산 오류 수정`
  - `Docs: README 실행 방법 추가`
  - `Refactor: QuizGame 책임 분리`
- `main` 이외의 브랜치를 최소 1회 생성하고, 해당 브랜치의 작업을 최소 1회 병합한다. `checkout`과 `merge` 기록이 확인되어야 한다.
- Git 명령어 `init`, `add`, `commit`, `push`, `pull`, `checkout`, `clone`을 각각 1회 이상 사용한다.

### 6.3 권장 기능별 커밋 시점

다음 작업을 완료할 때마다 커밋한다.

1. 저장소 초기 설정
2. 메뉴 기능
3. `Quiz` 클래스
4. 기본 퀴즈 데이터
5. 별도 브랜치의 퀴즈 풀기 기능
6. 퀴즈 추가 기능
7. 퀴즈 목록 기능
8. 점수 기능
9. `QuizGame` 클래스 구조 정리
10. 파일 저장 및 불러오기
11. README 작성 및 최종 수정

위 목록은 커밋을 기능 단위로 나누기 위한 기준이며, 실제 작업 과정에 따라 더 세분화할 수 있다.

### 6.4 `clone` 및 `pull` 실습

퀴즈 게임 개발을 완료한 후 다음 순서대로 진행한다.

1. 미션 저장소를 별도의 로컬 디렉터리에 `clone`한다.
2. 복제한 저장소에서 README 한 줄 추가와 같은 간단한 변경을 수행한다.
3. 변경 사항을 `commit`하고 `push`한다.
4. 기존 로컬 작업 디렉터리에서 `pull`해 변경 사항을 가져온다.
5. 가져온 내용이 정상적으로 반영되었는지 확인한다.

## 7. README 필수 항목

`README.md`에 다음 내용을 포함한다.

- 프로젝트 개요
- 퀴즈 주제와 해당 주제를 선정한 이유
- 실행 방법(예: `python main.py`)
- 기능 목록(퀴즈 풀기, 추가, 목록, 점수 확인 등)
- 파일 구조
- 데이터 파일 설명
  - `state.json`의 경로
  - 파일의 역할
  - 필드 또는 스키마 구조

실행 화면 이미지를 포함할 경우 다음과 같은 구조를 사용할 수 있다.

```text
docs/screenshots/menu.png
docs/screenshots/play.png
docs/screenshots/add_quiz.png
docs/screenshots/score.png
```

## 8. 선택 보너스 과제

다음 기능은 필수가 아니며 선택해서 구현할 수 있다.

### 8.1 랜덤 출제

- 퀴즈를 풀 때 문제 순서를 무작위로 섞는다.
- Python `random` 모듈을 활용한다.

### 8.2 문제 수 선택

- 퀴즈 시작 시 사용자가 풀 문제 수를 선택할 수 있게 한다.

### 8.3 힌트 기능

- `Quiz` 클래스에 힌트 속성을 추가한다.
- 풀이 중 힌트를 확인할 수 있게 한다.
- 힌트를 사용하면 점수가 차감되도록 한다.

### 8.4 퀴즈 삭제

- 등록된 퀴즈를 삭제할 수 있게 한다.
- 삭제 결과를 데이터 파일에 반영한다.

### 8.5 점수 기록 히스토리

- 최고 점수뿐 아니라 모든 게임 기록을 저장한다.
- 각 기록에 날짜와 시간, 푼 문제 수, 점수를 포함한다.

## 9. 완료 확인 체크리스트

### 9.1 프로그램

- [ ] Python 3.10 이상에서 외부 라이브러리 없이 실행된다.
- [ ] 실행 시 메뉴가 표시되고 5개 메뉴가 동작한다.
- [ ] 선택한 주제의 기본 퀴즈가 5개 이상 포함되어 있다.
- [ ] 퀴즈 풀기, 추가, 목록, 점수 확인이 정상 동작한다.
- [ ] 빈 값, 숫자가 아닌 값, 범위를 벗어난 값을 처리한다.
- [ ] `KeyboardInterrupt`와 `EOFError` 발생 시 가능한 데이터를 저장하고 안전하게 종료한다.
- [ ] 데이터 파일이 없거나 손상되어도 기본 데이터로 실행할 수 있다.
- [ ] 프로그램을 종료하고 다시 실행해도 추가 퀴즈와 최고 점수가 유지된다.

### 9.2 구조와 데이터

- [ ] `Quiz`와 `QuizGame`을 포함해 클래스가 2개 이상 정의되어 있다.
- [ ] 기능별 함수 또는 메서드가 분리되어 있다.
- [ ] 프로젝트 루트의 `state.json`에 퀴즈와 최고 점수가 저장된다.
- [ ] `state.json`을 UTF-8 인코딩으로 읽고 쓴다.
- [ ] 파일 관련 오류를 `try/except`로 처리한다.

### 9.3 Git과 문서

- [ ] GitHub 공개 저장소에 프로젝트가 업로드되어 있다.
- [ ] 의미 있는 커밋이 10개 이상 존재한다.
- [ ] 별도 브랜치 생성과 `main` 병합 기록이 각각 1회 이상 존재한다.
- [ ] Git 기초 명령어 7종을 각각 1회 이상 사용했다.
- [ ] 별도 디렉터리에서 `clone`한 뒤 변경 사항을 `push`하고 기존 디렉터리에서 `pull`했다.
- [ ] README에 요구된 6개 항목이 모두 포함되어 있다.
- [ ] 저장소 URL과 요구된 화면 캡처를 준비했다.

## 10. 참고 실행 흐름

아래 내용은 출력 문구나 디자인의 정답이 아니라 동작 확인을 위한 참고 흐름이다.

1. 프로그램을 실행하면 퀴즈 풀기, 추가, 목록, 점수 확인, 종료 메뉴가 나타난다.
2. 퀴즈 풀기를 선택하면 저장된 문제와 선택지 4개가 차례로 표시된다.
3. 답을 입력할 때마다 정답 여부를 알려주고, 전체 풀이 후 정답 수와 점수를 표시한다.
4. 기존 기록보다 높은 경우 최고 점수를 갱신한다.
5. 퀴즈 추가에서 문제, 선택지 4개, 정답 번호를 입력하면 저장 완료 메시지가 나타난다.
6. 목록에서 새 퀴즈가 보이고, 점수 메뉴에서 최고 기록을 확인할 수 있다.
7. 프로그램을 다시 실행해도 추가한 퀴즈와 최고 점수가 유지된다.
8. 잘못된 입력에는 허용 범위를 안내하고 재입력을 받는다.
